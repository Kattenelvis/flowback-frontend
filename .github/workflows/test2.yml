name: Test 2

# on:
#     push:
#         branches: ["master"]

on: push

jobs:
  build:
    name: Hello world action 2
    runs-on: ubuntu-latest
    steps:
    # - name: fkirst step
    - uses: actions/checkout@v3
    - run: 'echo "Hello World 2 Yay :3"'

    # - name: second step
    # - uses: ./.github/workflows/test.yml