name: Test 2

# on:
#     push:
#         branches: ["master"]

on: 
  push:
    branches: [ "yaml" ]
  schedule:
    - cron: "20 17 * * *"

jobs:
  
  test:
    strategy:
      matrix:
        os: [ubuntu-latest, windows-latest]
    name: Hello world action 2
    runs-on: ${{ matrix.os }}
    steps:
    - uses: actions/checkout@v3
    
    - name: fkirst step
      run: 'echo "Hello World 2 Yay :3 ${{ matrix.os }}"'

    - name: Show event trigger
      run: echo "Triggered by ${{ github.event_name }}"

    # - name: second step
    # - uses: ./.github/workflows/test.yml