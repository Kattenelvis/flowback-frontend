name: Test

# on:
#     push:
#         branches: ["master"]

on: push

jobs:
  build:
    name: Hello world action
    runs-on: ubuntu-latest
    steps:
    - uses: actions/checkout@v2
    - name: Built it!
      run: npm run build
