name: Test

# on:
#     push:
#         branches: ["master"]

on: push

jobs:
  build:
    name: Hello world action
    runs-on: ubuntu-latest
    steps:
    - uses: ./.github/workflows/test2.yml
    - uses: actions/checkout@v3
